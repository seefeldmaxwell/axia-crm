# SPA-style rewrites for dynamic [id] routes not pre-rendered at build time.
# Static files take priority; only unmatched paths hit these rules.
# The client-side React code reads useParams() from the actual URL.
/contacts/*  /contacts/c1  200
/accounts/*  /accounts/a1  200
/deals/*     /deals/d1     200
/leads/*     /leads/l1     200
/cases/*     /cases/cs1    200
