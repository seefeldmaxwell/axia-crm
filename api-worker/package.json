{
  "name": "axia-crm-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute axia-crm --local --file=./schema.sql",
    "db:seed": "curl -X POST http://localhost:8787/api/seed"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.57.0",
    "typescript": "^5.4.0"
  }
}
